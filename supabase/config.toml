project_id = "fnlgeeuohvethmfpsxpf"

[functions.send-room-setup-emails]
verify_jwt = false

[functions.send-guest-summary]
verify_jwt = false

[functions.ensure-guest-profile]
verify_jwt = false
[functions.payment-reminders]
verify_jwt = false

# Cron quotidien avec x-cron-key (ou JWT admin vérifié dans le code).
[functions.email-rules-run]
verify_jwt = false

# Stripe ne peut pas envoyer de JWT — la sécurité vient de la signature stripe-signature.
[functions.stripe-webhook]
verify_jwt = false

# GET = lien de paiement signé (HMAC cron_key) cliqué depuis un email, sans JWT.
# Le POST garde son contrôle d'auth manuel (getUser) dans le code.
[functions.stripe-checkout]
verify_jwt = false

[functions.revolut-bar-sync]
verify_jwt = false

# GET = callback OAuth Revolut (redirection après consentement, sans JWT).
# Le POST garde son contrôle d'auth manuel (x-cron-key ou JWT admin) dans le code.
[functions.revolut-sync]
verify_jwt = false

# Page publique investisseurs : auth par token secret (app_settings.investor_share)
# vérifié dans le code — lecture seule, agrégats uniquement.
[functions.investor-report]
verify_jwt = false

# Rapprochement virements bancaires -> echeances (auth x-cron-key ou JWT admin
# verifies dans le code, appel par cron horaire apres revolut-sync).
[functions.bank-payment-match]
verify_jwt = false

# Identification des payouts Stripe (auth x-cron-key ou JWT admin dans le code).
[functions.stripe-payout-enrich]
verify_jwt = false
